// Stub file: satisfies Metro bundler's resolution of ../../App from expo/AppEntry.js
// in the monorepo setup where node_modules is hoisted to the root.
// The actual app entry is apps/mobile/index.js (expo-router/entry).
import 'expo-router/entry';
